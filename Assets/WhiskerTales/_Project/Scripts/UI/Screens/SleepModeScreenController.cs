namespace WhiskerTales.UI.Screens
{
    public sealed class SleepModeScreenController : BackNavScreenBase
    {
        // V2-13 minimum: title + back + ambient body. Real sleep mode (reward timer, cat purring
        // audio, dim overlay) lives in a later phase. Tab placeholder for the Home bar.
    }
}
