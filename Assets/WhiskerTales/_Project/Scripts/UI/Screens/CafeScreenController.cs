namespace WhiskerTales.UI.Screens
{
    public sealed class CafeScreenController : BackNavScreenBase
    {
        // V2-6 minimum: title + background + back. Cafe restoration cards arrive in a later phase
        // alongside the legacy CafeRestoration adapter migration.
    }
}
